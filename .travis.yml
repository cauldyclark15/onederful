# telling docker we need super user access
sudo: required
services:
  - docker

before_install:
  - docker build -t cauldyclark15/onederful:v1 -f Dockerfile.dev .

script:
  - docker run cauldyclark15/onederful:v1 npm run test -- --coverage
